## Imported Claude Cowork project instructions

Make a mac application, which can be used to make a poster; 
The poster is very simple, choose size (should prepare some basic, useful size for posting ig, weibo, rednote etc;), then choose a color as background color, then can add text (can choose font, and size); Then export to a image;

The app name just called Poster;
